"""Isonome — Agentic robotics framework with hierarchical cognitive architecture."""

__version__ = "0.1.0"

# Serialization schema version — bump when the on-disk format changes.
# All to_dict() outputs include this; from_dict() validates it for
# forward/backward compatibility and migration.
SERIALIZATION_SCHEMA_VERSION = 1
